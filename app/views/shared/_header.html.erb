<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
    <a class="navbar-brand" href="<%= root_path %>">Ben Trinh</a>

    <div class="form-inline my-2 my-lg-0 dropdown ml-auto">
        <% if current_user.present? %>
            <a class="nav-link text-dark dropdown-toggle text-decoration-none" href="#" id="navbarDropdown" role="button" data-toggle="dropdown">
                <%= current_user.first_name %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                <a class="dropdown-item text-decoration-none" href="#">Profile</a>
            <div class="dropdown-divider"></div>
                <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item text-decoration-none' %>
            </div>
        <% else %>
            <%= link_to 'Sign In', new_user_session_path, method: :post, class: 'text-dark text-decoration-none' %>
        <% end%>
    </div>
</nav>